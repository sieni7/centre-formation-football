/**
 * Domain-specific validation logic (e.g., Zod schemas).
 */
// import { z } from 'zod'

// export const ExampleSchema = z.object({
//   name: z.string().min(3),
// })

export const dummyValidator = {}
